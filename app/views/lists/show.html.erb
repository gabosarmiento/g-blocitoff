<table class="table table-striped table-bordered">
  <thead>
    <tr>
        <td colspan="2">
        <b><%= link_to "Lists", lists_path %> / <%= @list.name.titleize %></b>
        </td>
      </tr>
      <tr><td colspan="2"><%= form_for [@list, @todo] do |f| %>
        <%= f.text_field :description %>
        <%= f.submit "Add todo", class: "btn btn-primary " %>
        <% end %>
        </td>
      </tr>
    <tr>
      <th width="100">Description</th>
      <th width="50">Days left</th>
    </tr>
  </thead>
  <tbody>
  <%= form_for :todo, url: todos_complete_path(:list_id => params[:id]) do |f| %>

  <% @todos.each do |t| %>
    <tr>
      <td>
         <% if t.status == true %>
       <li style="color:grey;"> <%= check_box_tag "todos_checkbox[]", t.id %>  <strike><%= t.description %></strike> </li>
        <% else %>
       <li> <%= check_box_tag  "todos_checkbox[]", t.id %> <%= t.description %>  </li>
        <% end %>
      </td>
      <td>
      </td>
    </tr>
  <% end %>
  <tr><td colspan="2">
  <%= submit_tag("Complete Todos", :class=>"btn btn-success") %></td></tr>
  <% end %>
  </tbody>
</table>